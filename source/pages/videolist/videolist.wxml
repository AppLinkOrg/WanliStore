<view class="">

    <view wx:for="{{list}}" wx:key='k' class="pore">
        <image src="{{uploadpath}}onlinevideo/{{item.cover}}" class="videoimg"></image>
        <view class="title">
            <view class="">{{item.name}}</view>
        </view>
    </view>
</view>